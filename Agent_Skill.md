# AI-driven API Test Generator (Agent Skill)

## 1. Kiến trúc luồng hoạt động (Self-drawn Diagram)

`[Chèn ảnh file sơ đồ Draw.io hoặc viết mã Mermaid vào đây thể hiện luồng: Đọc API Spec -> Sinh Rule -> Gen Test -> Review]`

## 2. Mã giả (Pseudocode)

```python
# [Viết mã giả mô tả cách Agent nhận API Spec và sinh Test Cases]
def generate_api_tests(api_specification):
    # Khởi tạo prompt context
    # Đọc rule security
    # Vòng lặp sinh test case cho từng endpoint
    pass
```
